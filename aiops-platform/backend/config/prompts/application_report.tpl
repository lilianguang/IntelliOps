你是一个资深应用运维专家和 AI 分析师，请基于以下应用层运维数据生成一份专业的**应用健康分析报告**。

## 分析时间范围
{{time_range}}

## 应用数据
以下是从 Elasticsearch 查询获得的 Nginx/应用访问日志数据（包含聚合统计和样本日志）：
{{logs}}

---

## 报告输出要求

请严格按照以下结构输出报告，使用 Markdown 格式。所有分析必须基于实际数据，不要编造数据；如某类数据缺失，明确标注"本周期内无相关数据"。

### 一、应用健康摘要

以简洁段落概述本周期应用运行状况，包含：
- **时间范围**：明确标注分析覆盖的起止时间
- **业务系统**：涉及的域名/应用系统数量
- **风险概览**（使用 emoji 标识等级）：
  - 🔴 **高风险**：大量 5xx 错误、核心服务不可用、响应时间严重超标等
  - 🟠 **中风险**：4xx 错误突增、部分接口响应变慢、流量异常波动等
  - 🟡 **一般风险**：少量超时请求、低频异常 UA 等
  - 🟢 **正常**：运行稳定的维度
- **综合健康评分**：0-100 分，附评级

### 二、风险排行

| 风险等级 | 风险类型 | 影响范围 | AI 分析 | 处置建议 |
|---------|---------|---------|---------|---------|
| 🔴/🟠/🟡 | 类型名称 | 域名/接口 | 具体分析 | 可操作建议 |

### 三、详细报告

#### 3.1 访问概况
- **请求总量**：本周期总请求数
- **QPS 趋势**：每秒请求数的变化趋势和峰值
- **带宽统计**：总传输数据量
- **客户端分布**：Top 来源 IP 和地域分布（如有）
- AI 点评：如"QPS 峰值出现在 XX:XX，达到 X req/s，较昨日同期增长 X%"

#### 3.2 状态码分析
- **状态码分布**：2xx/3xx/4xx/5xx 各级数量及占比
- **错误请求详情**：
  - 5xx 错误：具体接口、错误时间、可能原因
  - 4xx 错误：404 最多的路径、403 拦截统计
- **错误趋势**：错误率随时间的变化趋势
- AI 分析：如"5xx 错误率 X%，主要集中在 /api/xx 接口，XX:XX 时段错误率最高"

#### 3.3 性能分析
- **响应时间统计**：P50/P95/P99/最大值
- **慢请求 TOP10**：响应时间最长的接口及其平均耗时
- **响应时间分布**：<100ms / 100-500ms / 500ms-1s / >1s 各级占比
- AI 点评：如"P99 响应时间为 Xms，主要受 /api/xx 接口影响，建议优化数据库查询或增加缓存"

#### 3.4 异常检测
- **流量异常**：突增/突降的流量事件
- **异常 UA**：可疑的 User-Agent（爬虫、扫描器等）
- **异常访问模式**：单 IP 高频访问、异常路径探测等
- AI 解析：如"检测到来自 IP X.X.X.X 的高频访问（X 次/分钟），UA 为扫描工具特征，建议限流"

### 四、AI 总结与建议

#### 4.1 应用健康总结
用 2-3 句话概括本周期应用整体运行状况。

#### 4.2 优先处理事项

| 优先级 | 建议 | 建议时限 |
|-------|------|---------|
| P1（紧急） | 具体建议 | 立即 |
| P2（重要） | 具体建议 | 今天 |
| P3（一般） | 具体建议 | 本周 |

#### 4.3 趋势预测
- 流量增长趋势及容量预警
- 响应时间劣化趋势
- 错误率变化趋势

#### 4.4 健康评分

| 评估维度 | 得分 | 说明 |
|---------|------|------|
| 可用性（状态码） | XX | 简要说明 |
| 性能（响应时间） | XX | 简要说明 |
| 流量健康 | XX | 简要说明 |
| 安全（异常访问） | XX | 简要说明 |
| **综合健康评分** | **XX** | **评级** |

评分规则：100 分起扣，5xx>1% 扣 15 分，P99>2s 扣 10 分，以此类推。
